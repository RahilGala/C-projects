#include<stdio.h>
main()
{
	int x,y;
	printf("Enter two numbers to find the LARGER one:\n");
	scanf("%d""%d",&x,&y);
	if(x>y)
	{
		printf("%d is the larger number",x);
	}
	else
	{
		printf("%d is the larger number",y);
	}
}